:80 {
    # Handle requests with /api and strip the prefix before proxying
    handle_path /api* {
        reverse_proxy back:5000
    }
    
    # Proxy all other requests to the front service
    reverse_proxy * front:3000
}